import * as React from 'react';
import "./infinite-loader.sass";

interface IInfiniteLoaderProps { }

const InfiniteLoader: React.FunctionComponent<IInfiniteLoaderProps> = (props) => {
    return (
        <div className="kshop-infinite-loader">
            
        </div>
    )
};

export default InfiniteLoader;