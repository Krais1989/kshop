import * as React from 'react';
import "./registration-modal.sass"

interface IRegistrationModalProps { }

const RegistrationModal: React.FunctionComponent<IRegistrationModalProps> = (props) => {
    return (
        <h1>RegistrationModal</h1>
    )
};

export default RegistrationModal;