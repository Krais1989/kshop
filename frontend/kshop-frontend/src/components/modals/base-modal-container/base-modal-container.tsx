import * as React from 'react';
import "./base-modal-container.sass"

interface IBaseModalContainerProps { }

const BaseModalContainer: React.FunctionComponent<IBaseModalContainerProps> = (props) => {
    return (
        <h1>BaseModalContainer</h1>
    )
};

export default BaseModalContainer;