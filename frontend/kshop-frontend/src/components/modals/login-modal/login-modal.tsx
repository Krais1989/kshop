import * as React from 'react';
import "./login-modal.sass"

interface ILoginModalProps { }

const LoginModal: React.FunctionComponent<ILoginModalProps> = (props) => {
    return (
        <h1>LoginModal</h1>
    )
};

export default LoginModal;